# === State / runtime (per-host, regenerated by CC) ===
cache/
channels/
daemon/
daemon.log
daemon.lock
daemon.status.json
downloads/
file-history/
history.jsonl
ide/
jobs/
paste-cache/
plans/
projects/
session-env/
sessions/
shell-snapshots/
tasks/
telemetry/
.last-cleanup
cleanup.log
gh-pr-status-cache.json
last-install.json
settings.local.json
hooks/*.log
hooks/*.state

# === Secrets — NEVER commit ===
.credentials.json
mcp-needs-auth-cache.json
stats-cache.json

# === Per-host caches / installed bins ===
mcp/node_modules/
ccline/ccline
ccline/.api_usage_cache.json

# === Plugins cache (auto-managed by CC) ===
plugins/cache/
plugins/data/
plugins/install-counts-cache.json
plugins/marketplaces/

# === Backups (rotated by prune timer) ===
backups/

# === Editor / OS ===
.DS_Store
*.swp
*~
pull.lock
pre-pull-backups/

# === Added 2026-06-06: ephemeral / secret-shaped runtime (were untracked & not ignored) ===
daemon-auth-status.json
daemon-auth-cooldown
scan-secrets/
logs/
*.bak
*.bak.*

# === Sandbox bind-mounts: /dev/null char-devices that surface as untracked and abort `git add -A` ===
.bash_profile
.bashrc
.gitconfig
.gitmodules
.mcp.json
.profile
.ripgreprc
.zprofile
.zshrc
.idea
.vscode

# === Internal per-host state / caches ===
.claude/
plugins/plugin-catalog-cache.json
